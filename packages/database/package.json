{
  "name": "@qqmc/database",
  "private": true,
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "prisma generate",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint . --fix && prettier --write .",
    "preinstall": "prisma generate"
  },
  "devDependencies": {
    "@qqmc/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "prisma": "^6.11.0",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@prisma/client": "6.11.0"
  }
}
